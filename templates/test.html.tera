<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>Blog</title>
    </head>
    <body>
        <h1>Users</h1>
        <ul>
        {% for user in users %}
            <li>{{ user.name }}</li>
        {% endfor %}
        </ul>

        <h1>Posts</h1>
        {% for post in posts %}
            <h2>{{ post.title }}</h2>
            <p>{{ post.text }}</p>
        {% endfor %} 
    </body>
</html>
